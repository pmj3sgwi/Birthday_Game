# 碰撞位置定義表 — retro_game.py

> 每次新增或修改物件時同步更新此檔。  
> **修改後請把新數值貼給 Claude，Claude 會更新 `retro_game.py` 並 commit。**  
> 直接改這個檔案不會自動同步到程式。

---

## 各房間玩家移動邊界（`ROOM_BOUNDS`）

> 對應 `retro_game.py` 中的 `ROOM_BOUNDS` dict。  
> 座標空間：虛擬 **320 × 240**，`player_size = 20`  
> - `max_x` 上限 = 320 − 20 = 300（右牆內縮越多數值越小）  
> - `max_y` 上限 = 240 − 20 = 220（底牆內縮越多數值越小）

| 房間 | min_x | max_x | min_y | max_y | 說明 |
|------|------:|------:|------:|------:|------|
| `living_room` | 40 | 268 | 75 | 195 | 待校正 |
| `bedroom`     | 50 | 270 | 70 | 195 | 待校正 |
| `bathroom`    | 70 | 260 | 120 | 180 | 待校正 |

---

## 座標系統

| 項目 | 值 |
|------|-----|
| 虛擬空間 | `320 × 240`（所有 Rect 數字均在此空間） |
| 視窗大小 | `960 × 760`（遊戲區 960 × 700） |
| 換算 x | 虛擬 x × **3.0** = 視窗 x |
| 換算 y | 虛擬 y × **2.917** ≈ 視窗 y |
| Rect 格式 | `(x, y, width, height)` = 左上角座標 + 寬高 |

---

## 牆面邊界常數

> 定義透視視角的後牆線位置，物件貼齊 `_ROOM_VPT` 這條線才符合視覺。

| 常數 | 目前值 | 說明 |
|------|--------|------|
| `_ROOM_VPT` | `26` | 後牆頂部 y（天花板接縫，物件應 ≥ 此值） |
| `_ROOM_VPY` | `70` | 後牆底部 y（地板起點） |
| `_ROOM_LWX` | `60` | 後牆左側 x |
| `_ROOM_RWX` | `260` | 後牆右側 x |
| `_PLAYER_MIN_Y` | `12` | 玩家可走到的最上方 y |

---

## 客廳（Living Room）

### 物件碰撞 Rect

| 變數 | 目前值 `(x, y, w, h)` | x 範圍 | y 範圍 | 說明 |
|------|----------------------|--------|--------|------|
| `desk_rect` | `(105, 32, 120, 80)` | 120–210 | 32–82 | 電視桌整體碰撞範圍 |
| `calendar_rect` | `(195, 50, 14, 12)` | 124–138 | 35–47 | 月曆圖示（= desk.x+4, desk.y+3） |
| `tv_rect` | `(145, 32, 40, 35)` | 145–185 | 32–67 | 電視螢幕 |
| `cabinet_rect` | `(25, 35, 50, 70)` | 20–48 | 35–85 | 左側抽屜桌 |
| `living_door_rect` | `(15, 120, 10, 50)` | 15–25 | 80–160 | 左牆門 → 臥室 |
| `bathroom_door_rect` | `(280, 120, 10, 90)` | 288–298 | 80–160 | 右牆門 → 廁所 |
| `main_door_rect` | `(225, 28, 52, 10)` | 140–192 | 30–48 | 上牆大門 |
| `sofa_rect` | `(90, 190, 130, 50)` | 100–200 | 210–260 | 沙發 |

### 電燈開關（1988 場景專用）

| 變數 | 目前值 | x 範圍 | y 範圍 | 說明 |
|------|--------|--------|--------|------|
| `_SW_NX, _SW_NY` | `258, 30` | — | — | 開關左上角座標 |
| `_SW_NW, _SW_NH` | `11, 12` | 258–269 | 18–30 | 開關寬高 |
| `light_switch_prox` | `(238, 30, 51, 50)` | 238–289 | 18–68 | 感應範圍 = `(NX-20, NY, NW+40, 50)` |

### 感應範圍（Proximity Rect）

> `cx` = `rect.centerx`，`cy` = `rect.centery`（pygame Rect 中心座標屬性）

| 物件 | inflate | 最終大小 |
|------|---------|---------|
| 月曆（calendar） | `calendar_rect.inflate(5, 110)` | 36×56 |
| 電視（tv） | `tv_rect.inflate(5, 100)` | 56×51 |
| cabinet | `cabinet_rect.inflate(16, 16)` | 56×66 |
| living_door | `living_door_rect.inflate(50, 0)` | 20×100 |
| bathroom_door | `bathroom_door_rect.inflate(70, 0)` | 30×100 |
| main_door | `main_door_rect.inflate(5, 100)` | 57×35 |

### 客廳櫃UI（Cabinet UI in retro_game.py）

> Cabinet UI 中的黃色選擇框設定（UI Dialog 內，非虛擬座標）

| 變數 | 說明 | 寬度調整 | 高度調整 |
|------|------|---------|---------|
| `黃色框寬度` | 從 drawer rect 寬度縮小 | `width / 2`（寬度減半） | `height * 0.8`（保留80%高度） |
| `邊框寬度` | 黃色框的邊框粗度 | `2` 像素 | — |

---

## 臥室（Bedroom）

### 物件碰撞 Rect

| 變數 | 目前值 `(x, y, w, h)` | x 範圍 | y 範圍 | 說明 |
|------|----------------------|--------|--------|------|
| `bedroom_door_rect` | `(280, 115, 18, 65)` | 302–320 | 142–207 | 右牆門 → 客廳 |
| `bookshelf_rect` | `(20, 30, 57, 97)` | 18–75 | 12–109 | 左側書架 |
| `computer_desk_rect` | `(78, 30, 139, 90)` | 78–217 | 12–102 | 電腦桌（上牆中央） |
| `bed_rect` | `(180, 30, 100, 90)` | 120–220 | 30–120 | 床 |
| `iron_cabinet_rect` | `(18, 135, 57, 85)` | 18–75 | 120–205 | 鐵櫃 |

### 感應範圍（Proximity Rect）

| 物件 | inflate |
|------|---------|
| bedroom_door | `inflate(20, 20)` |
| bookshelf | `inflate(16, 16)` |
| computer | `inflate(16, 16)` |
| bed | `inflate(16, 16)` |
| iron_cabinet | `inflate(16, 16)` |

---

## 廁所（Bathroom）

### 物件碰撞 Rect

| 變數 | 目前值 `(x, y, w, h)` | x 範圍 | y 範圍 | 說明 |
|------|----------------------|--------|--------|------|
| `bathroom_exit_rect` | `(30, 77, 17, 83)` | 0–17 | 77–160 | 左牆門 → 客廳 |
| `pipe_rect` | `(87, 3, 94, 37)` | 87–181 | 3–40 | 上牆漏水管 |
| `sink_rect` | `(90, 75, 95, 85)` | 76–139 | 37–92 | 洗手台 |
| `toilet_rect` | `(185, 70, 30, 90)` | 146–202 | 40–95 | 馬桶（鐵箱/架子位置） |
| `mirror_rect` | `(110, 50, 63, 90)` | 55–145 | 12–47 | 鏡子（2026 only）待 F1 校正 |
| `bathtub_rect` | `(210, 70, 50, 130)` | 230–305 | 60–140 | 浴缸（2026 only）待 F1 校正 |

### 感應範圍（Proximity Rect）

| 物件 | inflate |
|------|---------|
| bathroom_exit | `inflate(50, 5)` |
| mirror | `inflate(24, 80)` |
| bathtub | `inflate(16, 16)` |
| pipe | `inflate(12, 12)` |
| toilet | `inflate(16, 16)` |

---

## 更新規則

新增或修改物件時，**必須同步更新**：

1. `retro_game.py` 中的 `pygame.Rect(...)` 定義
2. `_do_proximity_check()` 中的 proximity 判斷條件
3. SPACE 鍵處理器中的 `colliderect` 判斷
4. `DIALOGUE_MAP` 新增對應對話文字
5. `_trigger_action()` 新增動作邏輯
6. **本 `COLLISIONS.md` 對應表格**

---

*最後更新：Round 17 / commit `900a052`*
